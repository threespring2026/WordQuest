# WordQuest 问题收集

更新日期：2026-09-24

本目录记录内部测试中发现的问题和待验证的风险。当前开发先处理玩法、界面和学习体验；账号与安全改造放在正式上线前完成。记录问题不代表已经决定具体设计方案。

## 记录方式

- **已确认**：当前代码能直接证明问题存在。
- **待复测**：代码存在风险，需要在实际设备或完整游戏流程中复现。
- **待决定**：需要先确定产品体验，再选实现方式。
- 状态默认是「待处理」；修复后补充结果和验证日期。

## A. 当前内部测试阶段：软件本身

| 编号 | 问题与影响 | 依据 | 状态 |
| --- | --- | --- | --- |
| WQ-01 | AI 剧本只检查 `mapId`、`npcs`、`dialogues` 是否存在。游戏直接读取回合、选项和 NPC 数据；若生成内容缺轮次、选项或引用无效角色，可能无法继续游戏。需建立完整结构校验，并在失败时给出可恢复的处理。 | `api/_lib/ai.js`、`src/modules/game/index.js` | 已确认，待处理 |
| WQ-02 | 结算页「再玩一次」会回到故事生成页，重新生成剧情并再次等待；项目文档描述的是复用同一份配置。需先确定按钮含义，再使文案和行为一致。 | `src/modules/result/index.js` 的 `replay()` | 已确认，待决定 |
| WQ-03 | 故事生成虽然显示轮询进度，但首次查询状态的请求内直接完成两次 AI 调用；请求慢或中断时，玩家可能长时间停留在生成页。需在真实 AI 环境中测耗时、超时与重试体验。 | `api/story/status/[jobId].js`、`src/modules/story/index.js` | 待复测 |
| WQ-04 | 故事页每次进入都会新增一个 `visibilitychange` 监听，离开页面时未移除。多次重玩后可能重复查询任务。 | `src/modules/story/index.js` 的 `bindEvents()`、`unmount()` | 已确认，待处理 |
| WQ-05 | 图片从缓存加载时，地图可能重复初始化 NPC 和玩家。现已加一次性保护，本地缓存图片测试为 4 名 NPC、1 名玩家。 | `src/modules/game/index.js` 的 `render()`、`initMap()` | 2026-09-24 自测通过，待用户验证 |
| WQ-06 | 单词熟悉度用答案文本是否包含目标词来推断答题结果，不能可靠对应到实际被考查的单词。需明确每轮考查词，再更新熟悉度。 | `src/modules/result/index.js` 的 `saveResult()` | 已确认，待处理 |
| WQ-07 | 词书和游戏历史主要存在当前浏览器；换设备或清除浏览器数据后无法接续。内部测试可暂用，但需决定正式产品是否支持跨设备同步。 | `src/modules/wordbook/index.js`、`src/shared/api.js` | 已确认，待决定 |
| WQ-08 | 顶部音量按钮目前只切换图标和状态，未看到背景音乐播放逻辑。需决定补上音频，或在音频功能完成前调整此按钮。 | `src/modules/game/index.js` 的 `bindEvents()` | 已确认，待决定 |
| WQ-17 | 地图显示区域、碰撞通道、NPC 槽位与人物脚底坐标未统一：点击后人物可能走到视觉上的障碍或偏离目标点。现已按六张地图校正，并统一地图、点击和人物脚底坐标。 | `src/config/maps.config.js`、`src/modules/game/index.js`、`src/modules/map-editor/index.js` | 2026-09-24 自测通过，待用户验证 |
| WQ-18 | 试玩对话时，直接点击答案句子中的单词会打开词义弹窗，答案没有被选中；点击答案开头可正常选择。需要决定词义查看与答案选择如何兼容。 | `src/modules/game/index.js` 的 `renderOptions()`、`src/shared/ui.js` 的单词点击处理 | 已复现，待下一开发周期处理 |

## B. 正式上线前：账号、安全与稳定性

| 编号 | 问题与影响 | 依据 | 状态 |
| --- | --- | --- | --- |
| WQ-09 | AI 密钥写在会由浏览器加载的文件里，服务端也有同一密钥作为兜底值。上线前必须移除代码中的密钥并轮换。**该密钥已随当前代码发布，若仍有效且关联费用，轮换应提前进行。** 本清单不记录密钥原文。 | `src/config/ai.config.js`、`api/_lib/ai.js` | 已确认，待处理 |
| WQ-10 | 用户密码明文保存并直接比较；管理员登录存在默认密码。正式账号开放前需要改造密码存储和管理员配置。 | `api/auth/register.js`、`api/auth/login.js`、`api/admin-login.js` | 已确认，待处理 |
| WQ-11 | 游戏次数只在浏览器状态中扣减，后端故事接口没有按用户验证次数和限制调用量；目前不能依此实行真实配额。 | `src/modules/wordbook/index.js`、`api/story/start.js` | 已确认，待处理 |
| WQ-12 | 用户数据用一份 Blob JSON 整体读写，未配置 Blob 时退回内存；多请求并发或实例重启可能造成数据不一致。需要确定正式数据存储方案。 | `api/_lib/store.js` | 已确认，待处理 |
| WQ-13 | AI 生成的对话文本和部分用户资料被拼进 `innerHTML`，缺少统一的文本转义。正式开放用户输入与 AI 内容前应消除脚本注入风险。 | `src/modules/game/index.js`、`src/modules/auth/index.js` | 已确认，待处理 |

## C. 接手维护

| 编号 | 问题与影响 | 依据 | 状态 |
| --- | --- | --- | --- |
| WQ-14 | README、开发指南、进度文档仍以早期 Supabase / Mock 设计为主，与当前 Vercel Blob 和真实 AI 实现不一致，容易误导后续开发。 | `README.md`、`开发指南.md`、`PROGRESS.md` | 已确认，待处理 |
| WQ-15 | 仓库追踪了大量 `node_modules` 文件，本地已有该目录下的删除记录。整理 Git 忽略规则及依赖提交方式前，先确认这些记录是否由同步软件产生。 | Git 工作区、`.gitignore` | 已确认，待处理 |
| WQ-16 | `package.json` 目前只有本地启动命令，缺少可重复执行的游戏流程和接口测试。开始修改核心流程时补充针对关键风险的测试。 | `package.json` | 已确认，待处理 |

## 建议先处理的顺序

1. 先修复地图碰撞和人物行走（WQ-17），自测通过后等待用户复测。
2. 根据用户反馈，再确定「再玩一次」、音频、跨设备词书等体验设计（WQ-02、07、08）。
3. 稳定 AI 故事到游戏通关的主流程（WQ-01、03、04、05），校准学习反馈（WQ-06）。
4. 在正式上线前完成 B 类问题，再整理文档、依赖与测试（WQ-09～16）。

每次内部测试可直接在本文件追加新行，并记录复现步骤、设备、预期结果和实际结果。

## 本次地图自测记录（2026-09-24）

- 逐张查看六幅地图，核对角色脚底与白色站位标记、玩家起点和顶部角色提示。
- 六张地图均验证起点可到达四个 NPC 站位；对窄通道和主要障碍做了自动检查，3 项测试通过。
- 在浏览器中试玩森林、城镇、王宫、海港、废墟、学院地图的角色点击与自动行走，目标角色均能触发对话；缓存图片场景只有一组人物。
- 在地图编辑器查看森林通道预览及站位标记。真实设备上的触屏操作和不同屏幕尺寸仍需用户复测。
- 地图编辑器的「试玩当前地图」已验证可选择指定地图、完成六轮示例对话，并在结束或退出时返回原地图；试玩不请求 AI，也不会写入游戏结算。
