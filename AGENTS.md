# WordQuest 长期开发规则

本规则适用于整个 WordQuest 项目。以用户本轮和后续明确要求为准。

## 开发节奏

1. 持续了解整体产品、当前代码、项目文档和 `问题收集/README.md`，不要只看正在修改的文件。发现无关问题先记录，不在当前周期顺手开发下一个功能。
2. 每次只选择一个明确的功能或问题作为开发周期。修改前确认现状和预期行为；必要时先复现问题。
3. 功能完成后由开发者先自行测试。测试应覆盖本次改动的正常流程、相关边界情况和实际页面表现；记录通过的项目以及未覆盖的限制。未通过时继续修复和复测。
4. 自测通过后，向用户说明改了什么、如何测试、如何让用户复测，并等待用户亲自测试和反馈。收到用户确认之前，不开始开发下一个独立功能；用户反馈的问题继续归入当前周期修复。
5. 每完成一个较大的改动，自测通过后做一次 Git 版本提交并上传到 GitHub，便于用户测试部署版本。只提交本周期相关文件，保留已有的无关工作区变更。若上传失败，明确报告，不声称已上线。

## 当前阶段

- 项目仍处于内部测试。优先调整软件设计、地图与人物互动、故事流程、学习反馈和使用体验。
- 登录、账号安全和正式配额等问题记录在问题清单中，计划在正式上线前完成。已暴露且可能产生费用的密钥风险应及时提醒用户。
- 不使用真实付费 AI 调用做与 AI 无关的测试；地图与交互测试优先使用本地 Mock 故事。

## 问题与验证记录

- 新问题补充到 `问题收集/README.md`，标明「已确认」「待复测」或「待决定」。
- 修复后更新问题状态、验证方式和日期；待用户复测的问题标为「待用户验证」，不要提前标成最终完成。
- 项目文档与实际实现冲突时，以当前代码和用户最新决定为准，并在相关开发周期内修正文档。
